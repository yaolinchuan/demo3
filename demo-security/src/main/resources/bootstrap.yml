spring:
  application:
    name: demo-security
server:
  port: 8080


info:
    name: ${spring.application.name}
    description: Swagger2Markup demo
    environment: ${spring.profiles.active}
    version: 1.0.0
logging:
  level:
    root: debug
